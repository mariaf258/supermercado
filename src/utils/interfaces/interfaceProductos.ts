export interface datosCards {
    id?: string
    image?: string
    name?: string
    amount?: string
    price?: string
    category?: string
}

export default class ProductosDefault implements datosCards {
    id?: "";
    image?: "";
    name?: "";
    amount?: "";
    price?: "";
    category?: "";
}